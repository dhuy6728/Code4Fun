{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Web Server",
            "type": "chrome",
            "request": "launch",
            "url": "http://localhost:8000",
            "webRoot": "${workspaceFolder}",
            "preLaunchTask": "start-server"
        }
    ]
}